# archive-tool — CLI standalone para ler/extrair .archive (RDAR) do Cyberpunk 2077 no macOS.
# Só `std` + `bwms-hashes` (crate-folha local, zero-dep) como fonte única de hash. Offline, arm64.
[package]
name = "archive-tool"
version = "0.1.0"
edition = "2021"
rust-version = "1.74"
description = "Lê o índice de um .archive (RDAR) do Cyberpunk 2077, gera datamap.md e extrai conteúdo (Kraken pendente)."
authors = ["blackwall"]
license = "MIT OR Apache-2.0"

[[bin]]
name = "archive-tool"
path = "src/main.rs"

[dependencies]
# Fonte ÚNICA de hash do BWMS (crc32/crc64/sha1/fnv1a64/murmur3) — converge offline com o runtime.
bwms-hashes = { path = "../bwms-hashes" }

# Tabela [workspace] vazia: mantém o crate isolado de qualquer workspace pai
# (mods-research contém vários repos clonados; não queremos ser absorvidos).
[workspace]

[features]
# `kraken`: compila e linka o decodificador Kraken nativo (ooz, em ../ooz,
# adaptado p/ arm64) via build.rs, destravando extração comprimida e leitura de
# listas .kark. OPT-IN (2026-08-01: default removido — zero código de terceiro
# no build padrão, sempre). `cargo build --release` sozinho é 100% puro-std, zero
# toque em `ooz`/clang. Para extração comprimida, `cargo build --release --features kraken`
# (precisa de `clang++` e da fonte do ooz em ../ooz).
default = []
kraken = []

[profile.release]
opt-level = 3
lto = true
